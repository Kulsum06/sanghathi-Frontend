# Workspace Scripts

Operational scripts now live in the workspace root:
- `./scripts/`

Use this file as a pointer only. The canonical documentation is:
- `./scripts/README.md`

## Quick Commands (Run From Workspace Root)

- Start both backend and frontend:
  - `./scripts/start-servers.sh`
- Run all tests:
  - `./scripts/run-all-tests.sh`
- Export Atlas/source backup only:
  - `./scripts/backup-db-atlas.sh`
